<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- Background color, transparent in this case -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black"/> <!-- Border width and color -->
    <corners android:radius="4dp"/> <!-- Optional: adjust corner radius if needed -->
</shape>
